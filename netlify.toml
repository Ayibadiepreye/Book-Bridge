[build]
  command = "pnpm --filter @workspace/bookbridge run build"
  publish = "artifacts/bookbridge/dist/public"

[build.environment]
  NODE_VERSION = "24"
  PORT = "19905"
  BASE_PATH = "/"
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
